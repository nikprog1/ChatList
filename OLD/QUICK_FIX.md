# Быстрое решение проблемы "API-ключи не найдены"

## Проблема
Файл `.env.local` существует, но **ПУСТОЙ**. В нём нет переменной `OPENROUTER_API_KEY`.

## Решение (5 минут)

### Шаг 1: Откройте файл .env.local

```powershell
notepad .env.local
```

Или через любой другой редактор.

### Шаг 2: Добавьте строку с вашим API-ключом

Файл должен содержать **ТОЧНО** такую строку (без пробелов, кавычек, комментариев):

```
OPENROUTER_API_KEY=sk-or-v1-ваш_реальный_ключ_здесь
```

**ВАЖНО:**
- ❌ НЕТ пробелов вокруг знака `=`
- ❌ НЕТ кавычек вокруг значения
- ❌ НЕТ комментариев в строке
- ✅ Имя переменной: `OPENROUTER_API_KEY` (заглавными буквами)
- ✅ После `=` сразу идёт ключ

**Пример правильного формата:**
```
OPENROUTER_API_KEY=sk-or-v1-abc123def456ghi789jkl012mno345pqr678stu901vwx234yz
```

### Шаг 3: Сохраните файл

Нажмите `Ctrl+S` или выберите "Сохранить" в меню.

### Шаг 4: Перезапустите приложение

**ВАЖНО:** После изменения `.env.local` **ОБЯЗАТЕЛЬНО** перезапустите приложение!

1. Закройте приложение полностью
2. Запустите заново:

```powershell
python main.py
```

### Шаг 5: Проверка

После перезапуска приложения:
1. Перейдите на вкладку "Модели"
2. Убедитесь, что нужные модели активны (чекбокс "Активна")
3. Попробуйте отправить запрос

Если всё правильно, ошибка исчезнет.

## Где взять API-ключ OpenRouter?

1. Зайдите на https://openrouter.ai/
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел "Keys" (Ключи)
4. Создайте новый API-ключ
5. Скопируйте ключ (он начинается с `sk-or-v1-`)

## Проверка после настройки

Выполните проверку:

```powershell
python -c "from dotenv import load_dotenv; import os; load_dotenv('.env.local', override=True); key = os.getenv('OPENROUTER_API_KEY'); print('OK - ключ найден! Длина:', len(key) if key else 0) if key and len(key) > 10 else print('ERROR - ключ не найден или слишком короткий')"
```

Если выводит "OK - ключ найден!", значит всё настроено правильно.

## Типичные ошибки

### Ошибка 1: Пробелы вокруг =
```
❌ OPENROUTER_API_KEY = sk-or-v1-ключ
✅ OPENROUTER_API_KEY=sk-or-v1-ключ
```

### Ошибка 2: Кавычки
```
❌ OPENROUTER_API_KEY="sk-or-v1-ключ"
✅ OPENROUTER_API_KEY=sk-or-v1-ключ
```

### Ошибка 3: Комментарий в строке
```
❌ OPENROUTER_API_KEY=sk-or-v1-ключ # мой ключ
✅ OPENROUTER_API_KEY=sk-or-v1-ключ
```

### Ошибка 4: Неправильное имя переменной
```
❌ OPENROUTER_KEY=sk-or-v1-ключ
❌ openrouter_api_key=sk-or-v1-ключ
✅ OPENROUTER_API_KEY=sk-or-v1-ключ
```

### Ошибка 5: Пустое значение
```
❌ OPENROUTER_API_KEY=
❌ OPENROUTER_API_KEY=your_openrouter_api_key_here
✅ OPENROUTER_API_KEY=sk-or-v1-реальный_ключ
```

## Пример правильного файла .env.local

```
# Файл с переменными окружения для ChatList
# НЕ коммитьте этот файл в git!

OPENROUTER_API_KEY=sk-or-v1-abc123def456ghi789jkl012mno345pqr678stu901vwx234yz

# Дополнительные ключи (опционально):
# OPENAI_API_KEY=sk-...
# DEEPSEEK_API_KEY=sk-...
# GROQ_API_KEY=gsk_...
```

## После исправления

1. ✅ Файл `.env.local` содержит `OPENROUTER_API_KEY=ваш_ключ`
2. ✅ Формат правильный (без пробелов, кавычек)
3. ✅ Приложение перезапущено
4. ✅ Модели активированы на вкладке "Модели"
5. ✅ Запрос отправляется успешно

Если проблема сохраняется - проверьте, что вы **перезапустили приложение** после изменения файла!
