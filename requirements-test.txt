# Зависимости для тестирования приложения ChatList

# Основной фреймворк для тестирования
pytest>=7.4.0

# Плагины для pytest
pytest-qt>=4.2.0          # Для тестирования PyQt5 приложений
pytest-asyncio>=0.21.0     # Для асинхронных тестов
pytest-cov>=4.1.0          # Для проверки покрытия кода
pytest-mock>=3.11.1        # Для создания моков
pytest-timeout>=2.1.0      # Для ограничения времени выполнения тестов

# Библиотеки для моков HTTP запросов
respx>=0.20.0              # Для моков httpx (рекомендуется, так как используется httpx)
responses>=0.23.1          # Альтернативная библиотека для моков requests

# Генерация тестовых данных
faker>=19.0.0              # Для генерации реалистичных тестовых данных

# Моки времени и дат
freezegun>=1.2.2           # Для моков времени и дат в тестах
